---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: sudo-pair-archlinux
    image: archlinux/base
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: archlinux
verifier:
  name: goss
  lint:
    name: yamllint
